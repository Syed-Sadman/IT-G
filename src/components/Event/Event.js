import React from "react";
import { EventHeader, EventMaker } from "./Event.styled.js";
import { ButtonAll } from "../Button/Button.styled.js";

function Event() {
  return (
    <div>
      <EventHeader>ll</EventHeader>
      <ButtonAll>Add event</ButtonAll>
      <EventMaker></EventMaker>
    </div>
  );
}

export default Event;
